package uk.ac.brookes.alexrussell.chartinglibrary;

/**
 * Animation types for line graph. GENERAL applies no animation. FADE causes the visualization to fade into view. SLIDE slides the visualization
 * into view (useful for time series updates). See User Instructions.
 */
public enum AnimationType {
    GENERAL, FADE, SLIDE
}
